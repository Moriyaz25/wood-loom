import { NextResponse } from "next/server";
import cloudinary from "@/lib/cloudinary";
import { getAdminFromRequest, isSameOrigin } from "@/lib/auth";
import { rateLimit, unauthorized } from "@/lib/security";
export async function POST(request) { if (!isSameOrigin(request)) return NextResponse.json({ error: "Invalid origin" }, { status: 403 }); const admin = await getAdminFromRequest(request); if (!admin) return unauthorized(); if (!rateLimit(request, `upload:${admin.id}`, 30, 3600000)) return NextResponse.json({ error: "Upload limit reached" }, { status: 429 }); if (!process.env.CLOUDINARY_CLOUD_NAME) return NextResponse.json({ error: "Cloudinary is not configured" }, { status: 503 }); const form = await request.formData(); const file = form.get("file"); if (!file || !file.type?.startsWith("image/") || file.size > 8 * 1024 * 1024) return NextResponse.json({ error: "Choose an image under 8 MB" }, { status: 422 }); const buffer = Buffer.from(await file.arrayBuffer()); const uploaded = await new Promise((resolve, reject) => cloudinary.uploader.upload_stream({ folder: "woodloom/products", resource_type: "image", transformation: [{ width: 1800, height: 1800, crop: "limit", quality: "auto", fetch_format: "auto" }] }, (error, result) => error ? reject(error) : resolve(result)).end(buffer)); return NextResponse.json({ url: uploaded.secure_url, publicId: uploaded.public_id, width: uploaded.width, height: uploaded.height }); }
